# Module M10 – Testing

## What’s Covered
- Backend unit tests with Jest + Supertest (auth routes, protected routes, CRUD).
- React component tests with Jest + RTL (LoginForm interactions & validation).
- End-to-end tests with Selenium WebDriver (login happy path).

## How to Run
### Backend
```bash
cd server
npm test
npm run test:coverage