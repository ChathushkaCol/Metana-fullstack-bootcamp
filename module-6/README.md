✅ **module-6/** – PostgreSQL backend for Assignment M6: Node.js API with
users & blogs tables, database schema in `scripts/setup-db.sql`,
migration script `initDb.js`, and full CRUD REST endpoints using `pg`.

